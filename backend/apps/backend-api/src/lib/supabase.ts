/**
 * Legacy Supabase utilities — DEPRECATED
 * Platform migrated to local PostgreSQL + JWT auth.
 * This file is kept as a stub to prevent import errors.
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function verifySupabaseToken(_token: string): Promise<null> {
  // Supabase auth removed — platform uses local JWT
  return null;
}
