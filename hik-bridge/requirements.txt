# AION Hikvision Bridge — Python Dependencies
# HCNetSDK wrapper (native .so bindings via ctypes)
hikvision-sdk>=0.3.0

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data validation & settings
pydantic==2.10.0
pydantic-settings==2.7.0

# Redis pub/sub for event streaming
redis[hiredis]==5.2.1

# HTTP client for AION API calls
httpx==0.28.1

# Structured logging
structlog==24.4.0

# Environment loading
python-dotenv==1.0.1

# File serving
aiofiles==24.1.0
python-multipart==0.0.18

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
